# dsh-voice-call 任务生命周期修复记录（任务不播报 / 无法停止 / 任务会话无法完整打开）

> 本文件是给后续维护 agent / 开发者阅读的修复档案：记录 2026-09-08 一次针对「语音任务分发后
> 状态卡死、结果不播报、无法停止」问题的完整诊断过程、根因、修复内容与验证状态。

## 0. 元信息

| 项目 | 值 |
| --- | --- |
| 修复日期 / 时间 | 2026-09-08 20:36（UTC+8，本地时区） |
| 插件 | `@biliye/dsh-voice-call` v0.2.1（本仓库 `F:\xiangmu\project1`） |
| 运行环境 | DeepSeek Harness `@deepseek-ai/dsh` **0.1.2-rc.1**（npx 缓存：`C:\Users\123\AppData\Local\npm-cache\_npx\1e7f6d9597241db0`） |
| DSH_HOME | `C:\Users\123\.dsh`（web profile：`C:\Users\123\.dsh\profiles\web`） |
| 受影响文件 | `lib/index.js`（Host）、`lib/client.js`（Web Client） |
| 部署副本 | `C:\Users\123\.dsh\profiles\web\node_modules\@biliye\dsh-voice-call\lib\`（已同步，hash 与仓库一致） |
| 修改范围 | index.js +129/-16 左右，client.js +23/-2 左右（`git diff --stat`） |
| 验证 | 两文件 `node --check` 通过；运行时只读探针核实 Session API 与 agent 状态；临时探针均已移除 |

## 1. 用户报告的症状

1. 分发任务后，点击「打开任务会话」**不能完整显示任务流程**（打不开 / 看不到过程）。
2. **无法通过点击停止按钮停止该任务**（GUI 停止无效）。
3. **即使在主会话里说"停止/取消"也没有停止**（主会话助手自己都承认无取消手段）。
4. **任务完成后没有语音播报任务结果**，且任务列表长期显示"执行中"。

## 2. 诊断过程与关键证据

### 2.1 运行时证据（live，非猜测）

- 插件状态接口返回一个卡死任务：
  - `taskId = task-1788866538099-0`（"让QQ音乐播放《朋友的酒》"）
  - `sessionId = va-task-1788866538099-1`，`status = "running"`（持续数分钟不变）
  - 但 `result` 里已经出现了完整最终答案（"✅ 任务完成…已在 QQ 音乐中播放"）。
- 用只读动态 Cordis 插件查实时 agent 注册表：该 va-task agent **phase=idle**、`seq=68807`，
  即子代理早已正常跑完。
- 解压 `session.jsonl.zstd`（zstd 帧式存储，逐帧解码）确认会话日志尾部：
  - `assistant/message` → `step/end` → `turn/end reason={"kind":"completed"}`（11:37:51 UTC / 19:37:51 本地）。
  - 即：任务 agent 正常结束，但插件从未结算该任务。

### 2.2 根因一：Host 读 `agent.session.events`，但新版 DSH 运行时已移除该字段

- 插件多个函数读取 `agent?.session?.events`：
  - `lastAssistantText()`、`summarize()`、`personaInjected()`
  - `agent/status` 的"其他会话完成跟踪"
  - 巡检器里的上下文长度判断
- 动态探针实测：实时 `Session` 上 `eventsType = "undefined"`（**没有 `.events`**）；
  现役 API 是 `snapshotEvents()` / `ownEvents()` / 属性 `eventsSnapshot`（内部 `.log` 的只读快照）。
- 后果链：`lastAssistantText()` 恒返回空 → 无论 `agent/status` idle 路径还是 15s 巡检路径，
  `completeTask()` 永不触发 → 任务永久 `running` → 主会话收不到 followup → 无「已完成」语音播报；
  `summarize()` 也为空 → 共享记忆/续接摘要失效；人格幂等判断失效。
- 这解释了"旧任务（2026-08 及更早）能播报、现在不能"——Harness 运行时在 8/31→9/8 之间升级过，
  插件没有同步适配 Session API。

### 2.3 根因二：任务会话被标成 `origin=subagent`，GUI 无法按普通会话打开/停止

- 旧代码 `createTaskSession()` 的 meta：`{ origin: 'subagent', parentSession: ..., cwd }`，
  且未经平台的子代理目录注册。
- DSH Web 只把**普通会话**列入工作区会话列表并提供「打开 / 停止 / 实时流式查看」；
  子代理会话须经父会话目录投递（`session/agent-busy`、`subagent/catalog-diagnostic` 均指向此约束）。
- 因此面板"📂 打开任务会话"（`sessions.open(va-task-*)`）静默失败 / 内容不完整，
  GUI 上的停止按钮也被子代理路由拒绝。

### 2.4 根因三：全链路没有任何停止手段

- `voice_task` 工具动作只有 `create/status/list`，无 `stop/cancel`；
- 无 HTTP 停止路由；面板任务卡无停止按钮；
- 主会话日志里助手原话："voice_task 工具没有 cancel 动作…我没有办法直接取消这个任务"。

## 3. 修复内容

### 3.1 Host `lib/index.js`

1. **Session 事件读取兼容层**：新增 `sessionEvents(agent)`，依次尝试
   `s.events` → `s.eventsSnapshot` → `s.snapshotEvents()` → `s.ownEvents()`；
   替换上述 5 处 `.events` 读取。兼容旧版运行时（仍保留 `.events`）与新运行时。
2. **完成/停止判定以最后一次 `turn/end` reason 为准**：
   - 新增 `lastTurnEndReason(agent)` 与 `humanizeTurnEnd(reason)`。
   - `agent/status` idle 处理器与 15s 巡检器统一：
     - `completed` → `completeTask()`（播报"已完成"）；
     - `aborted + user` → `stopTask()`（播报"已停止"）；
     - `error / interrupted / max-tokens / blocked` → `failTask()`（播报失败原因）；
     - 兜底：无 turn/end 但有文本时维持旧的"有文本即完成"逻辑，空 idle 走超时失败。
   - 避免"用户停止 → agent idle → 被误判为成功完成"的经典竞态。
3. **任务会话改为工作区普通会话**：`createTaskSession()` 去掉
   `origin:'subagent'` 与 `parentSession`，meta 只保留 `cwd`（语音工作区目录）。
   新任务会话将出现在「语音通话」工作区会话列表中，可打开、可实时查看、可被 GUI 停止。
   （沿用 `attachSession` + `sessionTitle.rename`，与专属会话 `voice-call-main` 同型。）
4. **新增停止链路**：
   - `stopTask(t, reason)`：先置 `status='stopped'`（防竞态）→ `agent.cancel({kind:'user'})`
     → push `task stopped` 事件 → `main.followup`（唤醒主会话以便播报）→ `pruneTasks()`。
   - `stopTaskById(taskId)`：查找并校验任务在执行中后调用 `stopTask`。
   - 新 HTTP 路由：`POST /api/voice-call/task-stop` `{ taskId }`（常量 `API.taskStop`）。
   - `voice_task` 工具动作扩展 `stop`（`action enum: create/status/list/stop`）。
   - 主会话快速回复提示词（`VOICE_QUICK_REPLY_PROMPT`）新增第 3.5 条：
     用户要求停止时先用 `list` 找 `taskId`，再用 `stop` 停止。
5. `failTask()` 守卫从"completed/failed"改为"仅 running 可转移"，`completeTask()` 同理由
   `status !== 'running'` 守卫，防止 stopped/completed 被再次改写。

### 3.2 Client `lib/client.js`

1. 新增 API 常量 `taskStop = '/api/voice-call/task-stop'`。
2. 抽出 `refreshTasks()` 复用；新增 `stopTask(taskId)`（调 task-stop 后刷新列表）。
3. 任务卡：运行中的任务显示「⏹ 停止」（带"停止中…"禁用态）；新增 `stopped` 状态徽标样式
   与文案"已停止"；失败原因文案区分 `failed`（"失败原因: "）与 `stopped`（直接展示原因）。

## 4. 验证状态

| 项目 | 结果 |
| --- | --- |
| `node --check lib/index.js` / `lib/client.js` | 通过（exit 0） |
| 运行时 Session API 探针 | `eventsSnapshot`/`snapshotEvents` 可用，`.events` 不存在（据此设计兼容层） |
| 实时 agent 状态探针 | 卡死任务 agent `phase=idle`、`turn/end completed`（修复目标状态机已覆盖） |
| 部署同步 | 仓库与 `node_modules` 部署副本两文件 SHA-256 完全一致 |
| 临时诊断物 | 全部移除（动态探针插件已 undefine；`.debug/` 目录已删除） |

> ⚠️ **生效方式**：Host 端（`lib/index.js`）需**重启 DSH（web profile）**才会加载新代码；
> Client 端（`lib/client.js`）**刷新浏览器页面**即可（仍不生效时强制刷新一次）。
> 任务表只存于内存：重启后旧的卡死任务记录会消失，其任务会话仍保留在工作区可供查看；
> 修复只对重启后**新建**的任务生效。

### 4.1 2026-09-08 回归验证（Host 重启并加载新代码后实测）

重启 DSH 后运行中的插件已带新路由（`POST /api/voice-call/task-stop` 空 body 返回
`400 {ok:false,error:"taskId required"}`，旧代码无此路由）；工作区任务列表已出现修复后
新建并成功完成的真实任务（「打开QQ」`status=completed` 且带完整 result），随后执行两轮受控回归：

| 验证项 | 结果 |
| --- | --- |
| 任务 A（`task-1788871708939-2`，纯文本提示） | `running → completed`，事件流含 `[task] completed` 与 `[reply]`（主会话收到 followup 并回复 → 播报链路打通），`result` 文本正常回传 |
| 任务 B（`task-1788871746497-4`，长分析提示） | 执行中调 `POST /task-stop` → `status=stopped`，`failReason=用户已停止`；2s 后复查仍为 `stopped`（未被 idle+文本误判为 completed），事件流含 `[task] stopped` 与 `[reply]` |
| 新任务会话身份 | `va-task-1788871708939-3`、`va-task-1788871746497-5` 实时 `session.header` **无** `origin`/`parentSession`（普通工作区会话，preset=standard）——可在工作区列表打开/停止 |
| 回归副作用 | 测试任务均已完成/停止，无残留运行任务；探针动态插件已移除 |

> 注：任务 A 提示词经测试端 PowerShell 发送时中文被转成乱码（测试端编码问题，非插件缺陷），
> 不影响完成/播报链路结论；任务 B 提示词使用 ASCII 规避该问题。

## 5. 回归风险与注意事项（维护提示）

1. **API 漂移教训**：本插件曾在旧运行时用 `session.events`；Harness 0.1.2-rc.1 的 `Session`
   已不暴露 `.events`。后续维护读取会话一律走 `sessionEvents(agent)` 兼容层，
   不要在别处新写 `agent.session.events`。
2. **状态机约定**：任务状态流转应只由「最后一次 `turn/end` reason」驱动；
   不要仅凭 `agent.status === 'idle'` + 有文本就判定完成（停止/中断同样会 idle）。
3. **任务会话身份**：任务会话必须是普通工作区会话，**不要**再标 `origin:'subagent'`/`parentSession`，
   否则 GUI 打不开、停不了（子代理路由约束）。
4. **播报依赖链路**：任务结果播报 = `completeTask/failTask/stopTask` → `main.followup()`
   → 主会话（`voice-call-main`）被唤醒并回复 → `reply` 事件 → 前端 TTS。
   若未来改播报逻辑，保持这条链路即可；`inject()` 只排队不唤醒，不能用于结果播报。
5. **停止 vs 续接**：`rolloverTask()` 仍用 `agent.cancel({kind:'user'})` 停旧会话——与停止用户操作共用
   abort 语义，因此结算逻辑统一依赖 turn/end reason，而不是 cancel 的调用来源。
6. 若后续 DSH 运行时再次升级，请先在实时 Session 上做只读探测再改读取 API。
   本机可参考的探测方式：动态 Cordis 插件注册只读 HTTP 路由查看
   `ctx.get('agents').get(id).session` 的方法面（本次诊断即用此法）。
7. 会话日志为 zstd 帧式存储（`session.jsonl.zstd`），直接读取需逐帧解压
   （Node ≥22 可用 `node:zlib` 的 `zstdDecompressSync`，并处理多帧/torn tail）。

## 6. 修复摘要（改动点速查）

| 位置 | 改动 |
| --- | --- |
| index.js `sessionEvents()` | 新增 Session 事件兼容读取 |
| index.js `lastTurnEndReason()` / `humanizeTurnEnd()` | 新增 turn/end 结算依据 |
| index.js `failTask/completeTask` | 守卫收紧为 `status==='running'` |
| index.js `stopTask` / `stopTaskById` | 新增停止逻辑（`stopped` 状态） |
| index.js `createTaskSession` | 去掉 subagent/parentSession，改普通工作区会话 |
| index.js `agent/status` + 巡检器 | 完成/停止/失败按 turn/end reason 结算 |
| index.js 路由 | 新增 `POST /api/voice-call/task-stop` |
| index.js `voice_task` | 新增 `action=stop`；主会话提示词新增停止指引 |
| client.js API | 新增 `taskStop` |
| client.js 任务卡 | 新增「⏹ 停止」按钮、`stopped` 徽标/文案、`refreshTasks()` |
