# dsh-voice-call 专属会话无法加载修复记录（消息 source 契约 / 持久化快照形状）

> 本文件记录 2026-09-12 对「历史加载失败：voice-call-main」的定位与修复，供后续维护参考。

## 0. 元信息

| 项目 | 值 |
| --- | --- |
| 日期 | 2026-09-12 |
| 插件 | `@biliye/dsh-voice-call` v0.2.1（未升版本号） |
| 受影响文件 | `lib/index.js`（Host） |
| 数据修复 | `~/.dsh/sessions/--C-Users-123-.dsh-voice-call--/voice-call-main/session.jsonl.zstd` |
| 新增校验 | `.debug/verify-voice-call-contracts.mjs`（gitignore，不进 npm 包） |

## 1. 症状

GUI 报错：

```
历史加载失败：failed to observe session "voice-call-main":
@deepseek-ai/dsh-session-format-v0-to-v1 refuses this format v0 Session:
agent/inbox/spliced 410 inserted message source summary must be a string;
source v0 artifact remains unchanged (gateway/internal)
```

专属会话（`voice-call-main`，format v0）无法被格式目录迁移，历史读不出来；
插件自身的 `ensure` 也起不来（后续报 `session "voice-call-main" already exists`）。

## 2. 根因一：插件写出的消息 source 违反 released 契约

DSH 的运行时可查询契约（`ctx.sessionPersistence` / `MessageSourceMap`，已用 Inspect 在实机核对）：

```ts
type MessageSourceMap = { plugin: { kind: 'plugin'; plugin: string } & ContextFormed; ... }
type ContextFormed =
  | { form?: never } | { form: 'instructions' } | { form: 'catalog' }
  | { form: 'snapshot'; sections: readonly ContextSnapshotSection[] }
  | { form: 'notice'; summary: string }      // ← notice 必须携带 string summary
  | { form: 'relay' } | { form: 'recall' };
```

原实现是 `const pluginSrc = (form) => ({ kind: 'plugin', plugin: 'voice-assistant', form })`，
4 处 `pluginSrc('notice')` 都没有 `summary`。

- v0 写出时运行时**没有**校验消息 source，坏数据直接落盘；
- v0→v1 迁移是**强校验**：`pluginSourceValue()` 对 `form === 'notice'` 执行
  `stringValue(source['summary'], ...)`，缺失即抛
  `... inserted message source summary must be a string`；
- 迁移按记录顺序拒绝**整个会话**（不是跳过坏记录），所以历史与 resume 一起失效。
- 附带影响：`dsh-client-ui-chat` 的 `noticeSummary()` 把非字符串/空 summary 视为
  opaque —— 这些 notice 在聊天里本来就渲染不出折叠行。

## 3. 根因二：`sessionPersistence` 快照 API 跨版本漂移，resume 分支失效

会话 id 在 `snapshot.header.id`（`SessionPersistenceSnapshot { header; revision; ... }`）——
这一点两个版本一致；**变的是「取快照」的方法名与 `list()` 的返回**：

| | dsh@0.1.1-rc.2（旧，`SESSION_FORMAT_VERSION = 0`） | dsh@0.1.5-rc.1（新，当前运行） |
| --- | --- | --- |
| `list(signal?)` | `Promise<SessionHeader[]>`（**扁平 header**） | `Promise<readonly SessionPersistenceSnapshot[]>` |
| `listSnapshots(signal?)` | 存在，返回 `SessionPersistenceSnapshot[]` | **已移除** |
| `stat(id)` | — | `Promise<SessionPersistenceSnapshot \| undefined>` |

原实现 `headers.some((h) => h.id === VOICE_SESSION_ID)` 是**按旧版 `list()` 契约写的**：
在 0.1.1 上正确，升到 0.1.5 后 `list()` 改为返回快照，`h.id` 恒为 `undefined`。

于是**重启后永远判定「未持久化」而走 `agents.create()`**：
身份已被占用 → `SessionAlreadyExistsError: session "voice-call-main" already exists`
（首次尝试留下写句柄声明后，后续重试必然撞这个错）。
「跨重启保持同一会话」的能力在新版上一直没生效。

**修法（同时兼容两个版本）**：特性探测方法名，两条路径读到同一种快照形状。

```js
const listSnapshots = typeof persistence.listSnapshots === 'function'
  ? persistence.listSnapshots.bind(persistence)   // 旧版：快照在这里
  : persistence.list.bind(persistence)            // 新版：list 已返回快照
const snapshots = await listSnapshots()
persisted = snapshots.some((s) => s?.header?.id === VOICE_SESSION_ID)
```

三种写法在两个版本上的行为（已实测，见 §6）：

| 写法 | 0.1.1-rc.2 | 0.1.5-rc.1 |
| --- | --- | --- |
| A 原始 `list()` + `h.id` | resume ✅ | resume ❌（恒 false） |
| B 仅 `list()` + `header.id` | resume ❌ | resume ✅ |
| **C 特性探测（当前）** | **resume ✅** | **resume ✅** |

## 4. 修复

`lib/index.js`：

1. `pluginSrc(form, text)`：`form === 'notice'` 时由正文生成 `summary`
   （`trim` + 空白折叠 + 截断 200 字），其余 form 一律不带 `summary`；
   4 处调用点改为 `pluginSrc('notice', <正文或简短结论>)`。
2. `persistence` 查询改为特性探测 `listSnapshots ?? list`，再读 `s?.header?.id`（见 §3）。

## 5. 已损坏会话的数据修复（本次一次性执行，非代码路径）

`voice-call-main` 的 v0 归档里有两类违反契约的记录，必须就地归一化才能迁移：

| 类别 | plugin | 问题 | 条数 |
| --- | --- | --- | --- |
| `agent/inbox/spliced.inserted[].source` | `voice-assistant` | `form:'notice'` 缺 `summary` | 31 |
| `user/message.data.source` | `voice-assistant` | 同上 | 27 |
| `user/message.data.source` | `dsh-mnemon` | `form:'instructions'` 多带 `summary` | 3 |

- `dsh-mnemon` 是**另一个插件**（现已不在 `node_modules` 中，无法改其源码）；它同样违反
  `ContextFormed`，其 3 条记录一并按契约归一化（删除多余 key，正文不动）。
- 处理方式：逐行解析，只重写命中的 61 行；**其余 2272 行字节完全不变**（已断言），
  重新按 checksummed zstd frame 编码，原地替换（先备份）。
- 备份：`.debug/session-repair/voice-call-main.session.jsonl.zstd.pre-repair.bak`
- 复现/修复脚本：`.debug/session-repair/{lib,run,repair,scan}.mjs`

## 6. 验证证据

用 DSH 自带的格式目录（`@deepseek-ai/dsh-session-format-catalog`，currentVersion=3）离线复刻了两条真实路径：

- **历史路径**（= `startMigrationStream`/`MigratingJsonlRows`，`createRestore(..., {validation:'transformed'})` + `decodeRow` + `finish`）
- **发布路径**（= 写打开时的 `verifyCurrentGenerationInWorker`：`encodeCurrentHeader/encodeCurrentEvent`
  → `assertV3RowAdmission` 逐行准入 → 以 `validation:'current'` 重新读回）

结果：

| 输入 | 历史迁移 | 发布代（v3） |
| --- | --- | --- |
| 修复前归档 | 失败，seq 410，与用户报错逐字一致 | — |
| 修复后实机归档 | OK，1102 events | OK，1103 行，准入+回读通过 |

插件侧：`.debug/verify-voice-call-contracts.mjs`（对修复前代码 5 项失败、修复后全通过，
非空跑）。注意 `assertV3RowAdmission` 只属于**当前代**扫描器，不要加到历史行上
（历史 `request/header.data.header.system` 会被误判；历史路径不做该校验）。

### 6.1 跨 DSH 版本兼容性核查（0.1.1-rc.2 vs 0.1.5-rc.1）

本机同时存在两份检出（`_npx\de4831d60afe10da` = 0.1.1-rc.2，`_npx\1e7f6d9597241db0` = 0.1.5-rc.1），
逐项静态比对插件用到的面：

| 检查项 | 0.1.1-rc.2 | 0.1.5-rc.1 |
| --- | --- | --- |
| `sessionPersistence.list()` 返回 | **扁平 `SessionHeader[]`** | **`SessionPersistenceSnapshot[]`** ← 唯一破坏性差异 |
| `sessionPersistence.listSnapshots()` | 有 | 无 |
| `ContextFormed.notice` 要求 `summary: string` | 是 | 是（故 §2 的修法两版都对） |
| `SESSION_FORMAT_VERSION` | 0（无 `dsh-session-format*` 包） | 3（含 v0→v1→v2→v3 迁移链） |
| 插件 inject 的 14 个服务键 | 全部存在 | 全部存在 |
| `agents.create/resume/get`、`inject/followup/steer/cancel` | 有 | 有 |
| `workspaceRegistry.create/attachSession`、`sessionTitle.rename` | 有 | 有 |
| `tools.restrict`、`systemPrompt.section`、`agentPresets.composeFrom` | 有 | 有 |
| `subprocess.resolveExecutable/spawn/collected`、`sandboxPolicy.workspaceRoot` | 有 | 有 |
| `jobs.onJobDone`、`tokenMeter.measure`、`llm.resolveModelInfo`、`agentDefaultModel.currentSelection` | 有 | 有 |
| 安装机制 `dsh.bundle` / profile `bundles` / `dsh.client` + `/plugins/<id>/client.js` | 有 | 有 |

结论：**除 §3 那一处外，未发现其它破坏性差异**；写打开/迁移相关能力只在新版存在
（旧版 `SESSION_FORMAT_VERSION=0`，本就没有迁移这一步，故 §2 那类坏记录在旧版不会报错）。

三种查找写法的实测行为矩阵：

| 写法 | 0.1.1-rc.2 | 0.1.5-rc.1 |
| --- | --- | --- |
| A 原始 `list()` + `h.id` | resume ✅ | resume ❌（恒 false → 走 create） |
| B 仅 `list()` + `header.id` | resume ❌ | resume ✅ |
| **C 特性探测（已采用）** | **resume ✅** | **resume ✅** |

> 局限：这是**静态契约比对**，没有在旧版 DSH 里实跑插件（本机旧版只是 npx 缓存，未安装未运行）。
> 若真要在旧版上跑，建议先用 `dsh --profile web --dump-config` 确认行已挂载，再观察 ensure 是否 resume。

## 7. 维护注意事项

1. **插件消息 source 必须满足 `ContextFormed`**：`notice` 必带非空 `summary`，其他 form 不得带。
   写坏一条会让**整个会话**无法迁移，代价极高。
2. DSH 的 `sessionPersistence` 面**跨版本改过**（见 §3）：读 `list()` 前先想清楚是哪个版本；
   需要快照时用 `listSnapshots ?? list` 特性探测，会话 id 只在 `header.id`。
   改动这块时把 `.debug/verify-voice-call-contracts.mjs` 跑一遍（内含两个版本形状的行为用例）。
3. 部署（沿用既有流程）：改 `lib/index.js` 后需同步到
   `~/.dsh/profiles/web/node_modules/@biliye/dsh-voice-call/lib/index.js` 并**重启 DSH**；
   Host 插件不会热更新（本次已同步落盘，重启即生效）。
4. 重启后 `agents.resume()` 会在同目录发布 `session.v3.jsonl.zstd` 后继代；
   v0 源文件保持字节不变（本次修复后的那份即成为迁移源）。
5. 若再次遇到「历史加载失败」，先用 `.debug/session-repair/scan.mjs` 扫一遍
   全量 `plugin` source 契约，再决定是改插件还是归一化归档。
