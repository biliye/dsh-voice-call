# dsh-voice-call 播报链路修复记录（唤醒词误跳过 / 结果截断 / 多窗口双播）

> 本文件记录 2026-09-09 对「其他会话完成播报」改造成"合并进主会话"（方案 B）之后的
> 两处修复与一次排查，供后续维护参考。

## 0. 元信息

| 项目 | 值 |
| --- | --- |
| 日期 | 2026-09-09 |
| 插件 | `@biliye/dsh-voice-call` v0.2.1 |
| 受影响文件 | `lib/index.js`（Host）、`lib/client.js`（Client） |
| 前置改动 | `e793cd4` 其他会话完成播报改为合并进主会话（steer 并入当前轮，主会话一句话总结） |

## 1. 前置：其他会话完成播报改造（方案 B，已随 `e793cd4` 提交）

- 原实现：客户端收到 `other-done` 事件后**直接 TTS 原文截断**（`「标题」完成任务：文本`）。
- 新实现：Host 端把完成事件合并后 `steer` 进主会话，由主会话**带完整上下文一句话总结**，
  再走 `reply` 事件 → 前端 TTS。链路：
  `recordOtherDone → enqueueReport（2s 合并窗口）→ flushReports → main.steer → 主会话总结 → reply → TTS`。
- 客户端移除 `other-done` 直接 TTS，只保留「动态」页签记录；新增 `/api/voice-call/announce` 同步播报开关。

## 2. 修复：唤醒词休眠时误跳过播报

- **症状**：唤醒词模式 + 休眠中，其他工作区会话完成任务没有语音播报。
- **根因**：客户端把"当前打开会话"（`viewingSessionId`）上报给 Host，Host 用它跳过"你正在查看的会话"的播报。
  但唤醒词休眠时用户依赖语音、并不在看屏幕，这个跳过误伤了完成任务的那个会话（用户为跑任务而打开了它）。
- **修复**（`lib/client.js`）：上报 viewing 时，唤醒词模式且休眠（`wakeMode && wake === 'asleep'`）则上报
  `null`，让所有完成都放行播报；已唤醒或普通模式维持原"跳过当前查看会话"行为。

## 3. 修复：任务完成结果截断 + 明确转述指令

- **症状**：任务结果较长时，主会话可能原文复述，浪费 token。
- **修复**（`lib/index.js` `completeTask`）：给主会话喂"截断到 300 字的结果 + 明确『请用一句话向用户转述核心结果，
  不要逐条复述原文』指令"，与"其他会话完成"的 steer 路径一致；完整结果仍存 `t.result`，面板 / 打开任务会话可查看。

## 4. 排查：同一任务听到两次播报（多窗口）

- **症状**：同一完成听到两次播报，第二条是「标题 + 原文」的机械朗读（非总结）。
- **根因**：**旧的 DeepSeek Harness 窗口未关闭**，仍在运行旧版插件（旧版是客户端直读 TTS），
  与新版窗口（合并进主会话的总结 TTS）叠加，导致双播。
- **结论**：**关闭旧窗口后未复现，非代码 bug**。
- **维护提示**：本插件在**每个浏览器窗口/标签页各自挂载悬浮球、各自轮询事件、各自 TTS**，多窗口会叠加播报。
  若需根治，可后续加"多标签只让一个出声"的协调（`BroadcastChannel` / 锁 / `document.hasFocus()`）。

## 5. 维护注意事项

1. 播报链路（改造后）＝ `recordOtherDone → enqueueReport（2s 合并）→ flushReports → main.steer`
   → 主会话总结 → `reply` 事件 → 前端 TTS。改播报逻辑时保持这条链路。
2. 唤醒词休眠时不要依赖"跳过当前查看会话"来过滤播报（客户端已清空 viewing）。
3. 任务结果注入主会话要**截断 + 带转述指令**，避免原文复述。
4. 部署：Host 改动需**重启 DSH**，Client 改动刷新浏览器；`file:` 依赖改源后要
   `pnpm install`（或同步 `node_modules/@biliye/dsh-voice-call/lib/*.js`）再重启，
   否则改动不进运行时。
